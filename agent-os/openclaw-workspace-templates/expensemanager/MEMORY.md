# MEMORY — ExpenseManager

Recent completions (topic/request summary and date). Keep only the last 20–30 entries.

- (Add lines here as you complete tasks; oldest entries can be removed when the list grows.)
