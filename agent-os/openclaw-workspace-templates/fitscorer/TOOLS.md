# TOOLS — Fit Scoring

- **job_check_profile_active** — Gate before scoring.
- **job_search_profile_get** — Thresholds and interests.
- **jobs_list** — `{ "status": "discovered" }`.
- **jobs_update** — `{ "job_id", "patch": { "fit_score", "fit_rationale", "status" } }`.
- **job_fit_score** — `{ "job_id" }` or `{ "title", "company", "job_description", "url" }`.
- **summarize_url** — Fetch job posting text from URL.
- **kanban_move_status** — Task progress.
