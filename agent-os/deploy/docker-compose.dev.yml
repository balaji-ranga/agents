# Dev override — HTTP only, no TLS certs required
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  nginx:
    ports:
      - "${HTTP_PORT:-8080}:80"
    volumes:
      - ./nginx/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro,Z
